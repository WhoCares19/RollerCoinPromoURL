# -*- mode: python ; coding: utf-8 -*-

block_cipher = None

a = Analysis(
    ['RollerPromoUrl.py'],
    pathex=[],
    binaries=[],
    datas=[('rcicon.ico', '.')],
    hiddenimports=[],
    hookspath=[],
    hooksconfig={},
    runtime_hooks=[],
    excludes=[],
    win_no_prefer_redirects=False,
    win_private_assemblies=False,
    cipher=block_cipher,
)

pyz = PYZ(a.pure, a.zipped_data, cipher=block_cipher)

exe = EXE(
    pyz,
    a.scripts,
    a.binaries,
    a.zipfiles,
    a.datas,
    [],
    name='Rollercoin Promo Launcher',
    debug=False,
    bootloader_ignore_signals=False,
    strip=False,
    upx=False,
    upx_exclude=[],
    runtime_tmpdir=None,
    console=False,  # <- hides terminal window
    icon='rcicon.ico',  # <- custom icon
    version='version.txt',  # <- metadata for file properties
)

coll = COLLECT(
    exe,
    a.binaries,
    a.zipfiles,
    a.datas,
    strip=False,
    upx=False,
    upx_exclude=[],
    name='Rollercoin Promo Launcher'
)
